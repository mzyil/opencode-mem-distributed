services:
  pgvector:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_PASSWORD: testpass
      POSTGRES_DB: memtest
    ports: ["55432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d memtest"]
      interval: 1s
      timeout: 5s
      retries: 30
